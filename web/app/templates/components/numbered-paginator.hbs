<ul class="pagination">
  {{#each pages as |page|}}
    {{#if (eq current page)}}
        <li class="active">
            <a href="#">{{page}}</a>
        </li>
    {{else}}
        <li>
            <a href="#" {{action 'changePage' page}}>{{page}}</a>
        </li>
    {{/if}}
  {{/each}}
</ul>
